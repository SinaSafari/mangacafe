import 'package:flutter/material.dart';

class StoreTab extends StatelessWidget {
  const StoreTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("store tab"),
      ),
    );
  }
}
